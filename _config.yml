# GitHub Pages (legacy Jekyll build): pages are plain static HTML, nothing is generated.
# These entries are working files, not part of the public site.
exclude:
  - docs
  - supabase
  - tools
  - README.md
  - .github
